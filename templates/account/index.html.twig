{% extends "base.html.twig.html" %}

{% block title %}Mon compte - monde de basket{% endblock %}

{% block content %}
    <h1>Mon compte</h1>
    {% if is_granted ('ROLE_ADMIN') %}
        <small><a href="/admin">Accéder au backoffice</a></small><br/>
   {% endif %}

   Bienvenue {{ app.user.firstname }} dans votre compte.<br/>
   C'est dans cet espace que vous allez pouvoir gérer toutes vos informations personnelles.
   <hr>
   <div class="row">
        <div class="col-md-4">
          <div class="account-item text-center">
               <img src="{{ asset('assets/img/password.png') }}" alt="Modifier mon mot de passe" ><br/>
               <a href="{{ path('account_password') }}">Modifier mon mot de passe</a>
          </div>
        </div>

        <div class="col-md-4">
          <div class="account-item text-center">
               <img src="{{ asset('assets/img/address.png') }}" alt="Gérer mes adresses" ><br/>
               <a href="{{ path('account_address') }}">Gérer mes adresses</a>
          </div>
        </div>

        <div class="col-md-4">
          <div class="account-item text-center">
               <img src="{{ asset('assets/img/purchase.png') }}" alt="Mes commandes" ><br/>
               <a href="{{ path('cart') }}">Mes commandes</a>
          </div>
        </div>
   </div>
   <hr> 
<style>
     .account-item {
          background-color: #f1f2f6;
          padding: 70px 
     }
     .account-item a {
          texte-transform: uppercase;
          font-size: 13px;
          color: #4b6584;
          font-weight: bold;
     }
     .account-item a:hover {
          color: #778ca3;
          text-decoration: none;
     }
</style>

{% endblock %}