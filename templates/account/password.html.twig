{% extends "base.html.twig.html" %}

{% block title %}Modifier mon mot de passe - monde de basket{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1>
   <a href="{{ path('app_account') }}">Retourner à mon compte</a><br/>
   <hr>
   {% if notification %}
    <div class="alert alert-info">{{ notification }}</div>
   {#{% else %}
    <div class="alert alert-warning">{{ notification }}</div>#}
    {% endif %}
   {{ form(form)}}
   <br/>
   <hr> 


{% endblock %}